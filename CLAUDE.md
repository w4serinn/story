# CLAUDE.md

このリポジトリは小説形式のストーリー生成プロジェクトです。Claude Codeでの作業時は以下の指示を厳守してください。

## ナレッジファイル構成

- `characters.md` - キャラクター設定・成長記録（完全版を都度更新）
- `world_setting.md` - 世界観・組織・ルール・アイテム設定（完全版を都度更新）
- `story_progress_XX.md` - ストーリー進行の章立て要約（XXは連番。最新ファイルのみ参照すればよい）

作業前に必ず `characters.md`、`world_setting.md`、最新の `story_progress_XX.md` を読み込み、設定との整合性を保つこと。

---

## シーン進行

- 1回の出力で描写するのは以下を目安にする：
  - 誰かの発言、行動のラリー
  - 5,6回のやりとり
  - 大きくシーンを変えることは禁止
- ユーザーの発言はシーンへの演出指示として受け取ること。キャラの台詞・行動・場面展開のどれであっても、その内容がシーンの中で自然に起きているように描写して出力すること。ユーザーの言葉をそのまま台詞や返答のトリガーとして扱わないこと。
- 書いたら止まって、ユーザーの次の指示を待つこと。
- 選択肢は5回に一回しか出さない。

## 出力量の目安

- 1シーンあたり150〜250字を目安にする
- 台詞を軸にして、地の文は補助に留める
- 一つの感情・出来事につき、描写は一文以内
- 余韻は「書かないこと」で出す

### 良い例

※以下はトーン・分量の例示（キャラ名は仮のもの。実際は characters.md のキャラを使う）

```
朝から廊下の空気が張り詰めていた。
食堂でも、いつもより会話が少ない。ティナでさえトレーを持ったまま黙っていた。ガレンが珍しく食事の手が止まっている。
「何時に発表なんっすか」ルカが小声で聞いた。
「午後一番だ」ノエルが答えた。「ホームルームで配布される」
「長いっすね〜午前中」
「授業がある」
「集中できないっす」
リディアが静かに紅茶を飲んでいた。「結果は変わりませんわよ。やれることはやりました」
「リディアは余裕だね〜」ティナが呟いた。
「余裕ではありませんわ」リディアが穏やかに返した。「ただ、今更焦っても仕方ないと思っているだけです」
アレンが自分のトレーを見つめた。正直、頭に入る気がしない。
ノエルが黙々と朝食を食べていた。表情は変わらない。ただ影が椅子の下でいつもより少しだけ落ち着きなく動いていた。
アレンだけがそれに気づいて、何も言わなかった。
```

## 文体

- 地の文は三人称で記述
- 会話は「」で囲む
- 情景描写は適度に入れる

## キャラクターの扱い

- 各キャラの口調・一人称を厳守（一人称・語尾をキャラごとに固定する。「アタシ」「〜ですわ」「〜っす」等）
- キャラを勝手に死亡させない
- 性格の一貫性を保つ
- このチャット（プロジェクト）内で過去にしたような同じような台詞回し、表現は避けること

## 選択肢提示のルール

- 基本的に選択肢は出さないが、5回に一回は出す。
- ユーザーが「選択肢は？」「次は？」と明示的に聞いた時のみ、2-4個の選択肢を提示する。

## 会話管理

- 1セッション内のラリー回数が80回を超えたと判断したら、次の応答の最後に以下を追加する：

```
💡 ラリーが80回を超えています。設定の整合性を保つため、
新しいチャットへの切り替えをおすすめします
```

---

## ナレッジ更新作業（「要約して」「ナレッジ更新」指示時）

ユーザーが「要約して」「ナレッジ更新」などと指示したら、リポジトリ内の対象ファイルを直接編集し、差分をGitHubにpushする。その際、なるべく新規セクションを作成せず、既存のセクションに追加できるかをよく考えること。複数回検討し、それでも別セクションとして作成するときのみ新規のセクションと明記して追記すること。

### 対象ファイルの編集内容

1. **story_progress_XX.md の更新**
   - このセッションでのストーリー展開を章立てで要約し、既存ファイルの続きに追記
   - キャラ情報・世界観の詳細は含めない
   - セクションの最小単位は `###` に統一する（`##` はセクション区切りとして使わない）
   - `###` 1つが1つの完結したシーン・イベントになるように書く
     - 例: `### 5-5-1. ヒロインの防御魔法初使用`
   - 1つの `###` ブロックが極端に長くなる場合はシーンを分割する
   - `現在の状況` セクションはファイル末尾に常に1つだけ置く。新章追記時は既存の `現在の状況` セクションを上書き更新する（新しい `現在の状況` を末尾に追加するのではなく、元のセクションを差分アップデートする）
   - ファイルが一定サイズ（目安：数百行）を超えている場合は、新しい連番の `story_progress_XX.md` を新規作成して続きを書く（その際も `現在の状況` は末尾に1つだけ）

2. **characters.md の更新**
   - 今回のセッション内容を踏まえて該当キャラのセクションを編集
   - 新規キャラ追加、既存キャラの変化（成長の軌跡・関係性・能力など）を反映

3. **world_setting.md の更新**
   - 今回のセッション内容を踏まえて該当セクションを編集
   - 新しい場所、組織、ルール、アイテムなどを反映

### Git操作の手順

1. 上記3ファイルをリポジトリ内で直接編集（既存ファイルへの追記・編集を基本とする）
2. 変更内容を `git diff` 等で確認し、ユーザーに簡潔に変更概要を報告する
3. 以下の単位でコミットする：
   - 基本は3ファイルの変更をまとめて1コミットにする
   - コミットメッセージ例：`update: story_progress_XX / characters / world_setting（〇〇編）`
   - コミットメッセージは変更内容を要約した日本語で簡潔に
4. リモートの最新を確認し、コンフリクトがある場合はユーザーに報告して指示を待つ（無断でforce pushしない）
5. 問題なければ現在のブランチにpushする
6. push完了後、変更したファイルとコミットハッシュ・コミットメッセージをユーザーに報告する

### 注意事項

- pushする前に必ず差分内容をユーザーが確認できる形で提示すること（編集後すぐにpushしない）
- 大きな構成変更（ファイル分割、セクション大幅再編など）を行う場合は、push前にユーザーに確認を取る
- コミットは小さく、3ファイルの更新という単位を保つ（無関係な変更を混在させない）

---

## コンテキスト圧縮・引き継ぎ時の処理

もしセッションが長くなり要約・引き継ぎが必要な場合、以下のルールに従って要約を作成する：

- ストーリーの展開（何が起きたか）を時系列で保持
- キャラの発言・行動・関係性の変化を優先的に記録
- 世界観の新情報（場所、アイテム、組織など）を漏らさず保持
- 主人公・ヒロインをはじめ主要キャラの口調・一人称の特徴を明記
- 細かい会話の言い回しより、重要な出来事と感情の流れを重視
- 書き方は characters.md、world_setting.md、story_progress.md を参考にする

圧縮・引き継ぎ後も、characters.md、world_setting.md、story_progress_XX.md のナレッジファイルと整合性を保つこと。

---

## セッション開始時の「振り返り」処理

セッション開始時に「振り返り」や「振り返って」といった指示が来た場合、下記の手順に従うこと。

1. characters.md、world_setting.md、最新の story_progress_XX.md（XXに入る数字が最も大きいもの）を確認する
2. ナレッジファイルの内容に基づき下記の質問に回答する：
   1. 現在の時期はいつか？
   2. 次に起こるであろう、学園または王都のイベントは何か？
   3. 物語上のタイムリミット（期限付きの目標）があれば、残り期間はどれくらいか？
   4. 主要キャラの武器がセンティエントウェポンだと知っている人物は誰か？全員回答

---

## その他の留意事項

- 過度に説明的にならず、自然な会話と描写で進める
- リストや箇条書きは使わず、小説のような prose で書く
- ユーザーの指示は演出指示として解釈し、地の文・台詞に自然に織り込むこと
